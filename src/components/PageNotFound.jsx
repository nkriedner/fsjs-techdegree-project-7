import React from "react";

const PageNotFound = () => {
    return (
        <>
            <h3>Page Not Found (404)</h3>
            <p>Sorry, but there is no page for the requested url.</p>
        </>
    );
};

export default PageNotFound;
